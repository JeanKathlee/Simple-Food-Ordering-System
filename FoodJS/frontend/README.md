# FoodJS frontend

The React client for FoodJS. It includes the public landing page, customer ordering flow, live order tracking, account screens, and the role-protected admin dashboard.

Run `npm run dev` for local development, `npm run lint` for static checks, and `npm run build` for a production bundle. API requests under `/api` are proxied to `http://localhost:3001` during development.
